# ASRE — Production Dependencies
# Install: pip install -r requirements.txt

# -- Web Framework ------------------------------------------------------------
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
gunicorn>=22.0.0          # Multi-worker process manager (production)

# -- AI Orchestration ---------------------------------------------------------
langgraph>=0.2.0
langchain-core>=0.2.0

# -- Database -----------------------------------------------------------------
duckdb>=0.10.0
pyarrow>=14.0.0
pandas>=2.0.0

# -- Validation ---------------------------------------------------------------
pydantic>=2.0.0

# -- Observability (optional, uncomment for production monitoring) ------------
# prometheus-fastapi-instrumentator>=6.0.0
# opentelemetry-sdk>=1.0.0
